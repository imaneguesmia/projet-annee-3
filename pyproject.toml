[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "projet-annee-3"
version = "0.1.0"
description = "Chess game with pygame and python-chess."
requires-python = ">= 3.13"

dependencies = [
    "pygame>=2.0.0",
    "nanobind>=2.5.0"
]